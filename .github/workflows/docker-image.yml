name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build and push the Docker image
      #run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
      run: | 
        echo "${{ secrets.DOCKERPW }}" | docker login -u "shuklaaditya846" --password-stdin
        docker image build -t shuklaaditya846/taskapp:v2 .
        docker push shuklaaditya846/taskapp:v2
